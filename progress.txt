## Phase 1: Minimal SSE Endpoint - COMPLETED

Commit: 54cb5d7

### What was built:
- Created streaming service module at `server/services/streaming/`
  - `streaming.types.ts` - Typed SSE event definitions (init, video, progress, complete, error)
  - `streaming.ts` - Helper functions to send typed events via Hono's SSE API
  - `index.ts` - Re-exports for clean imports
- Added `POST /api/brand-explorer/stream` endpoint in `server/index.ts`
  - Streams hardcoded fake data to validate SSE infrastructure
  - Demonstrates full event flow: init → video/progress (x3) → complete

### Acceptance criteria verified:
- [x] Returns `Content-Type: text/event-stream`
- [x] Streams `init`, `video`, `progress`, and `complete` events in correct order
- [x] Events properly formatted SSE (event/data fields)
- [x] Tested with curl - events arrive over time with delays
- [x] Streaming service module created with typed event helpers

---

## Phase 2: Streaming Brand Explorer with DB Persistence - COMPLETED

Commit: 7490442

### What was built:

**Brand Explorer Service (`server/services/brand-explorer/`):**
- Added `exploreBrandStreaming()` function that processes videos sequentially
- Added `ClassifiedVideo` and `StreamingCallbacks` types
- Exported `normalizeHandle()` function for reuse

**Brand Explorer History Service (`server/services/brand-explorer-history/`):**
- `createBrandExplorerSearchRecord()` - Creates Search record at stream start
- `saveStreamingVideo()` - Saves single video during streaming
- `finalizeBrandExplorerSearch()` - Creates summary record at stream end

**Streaming Endpoint (`server/index.ts`):**
- Updated `POST /api/brand-explorer/stream` to use real data
- Returns cached results as JSON immediately (no streaming overhead)
- For uncached: creates Search record → streams videos → creates summary

### Acceptance criteria verified:
- [x] Stream endpoint creates `Search` record and streams `searchId` in `init` event
- [x] Videos from Apify are classified sequentially (not in parallel)
- [x] Each classified video is saved to DB before streaming
- [x] `video` event contains full `SearchResultItem` data
- [x] `progress` event shows accurate `completed` / `total` counts
- [x] `complete` event contains correct summary (totalVideos, totalInfluencers, totalReach)
- [x] Summary record created in DB at stream end
- [x] Partial results visible in DB if stream interrupted mid-processing

---

## Phase 3: Frontend Streaming Hook & Integration - COMPLETED

Commit: 3aca1b8

### What was built:

**useStreamingSearch Hook (`app/hooks/useStreamingSearch.ts`):**
- Opens fetch connection to streaming endpoint
- Parses SSE events and accumulates results in state
- Handles both SSE streams and cached JSON responses
- Exposes: results, progress, summary, searchId, query, isPending, error
- Provides: startSearch, cancel, reset actions
- Cleans up on unmount via AbortController

**HomePage Integration (`app/routes/index.tsx`):**
- Replaced `useBrandExplorerMutation` with `useStreamingSearch`
- DataGrid receives streaming results and updates live
- History sidebar refreshes after `onComplete` callback
- Updates `currentSearchId` when `streamingSearchId` becomes available

**DataGrid Updates (`app/components/DataGrid.tsx`):**
- Added `progress` prop for streaming progress display
- Shows "Processing X of Y videos" in loading state
- Shows progress in info bar while streaming with data

### Acceptance criteria verified:
- [x] `useStreamingSearch` hook opens EventSource to streaming endpoint
- [x] Hook accumulates `results` array as `video` events arrive
- [x] Hook exposes `progress: { total, completed }` from progress events
- [x] Hook exposes `summary` after `complete` event
- [x] Hook exposes `searchId` immediately after `init` event
- [x] Hook exposes `isPending` (true while streaming, false after complete/error)
- [x] Hook cleans up EventSource on unmount
- [x] `HomePage` uses hook and DataGrid shows rows appearing incrementally
- [x] History sidebar refreshes after search completes

---

## Phase 4: Error Handling & Resilience - COMPLETED

Commit: a9d9a51

### What was built:

**Backend (already complete from Phase 2):**
- Single video classification failures call `onError` callback and continue processing
- Apify failures (all searches fail) throw `BrandExplorerError`
- Streaming endpoint catches errors and sends error events

**Frontend Hook Updates (`app/hooks/useStreamingSearch.ts`):**
- Added `VideoError` type and `videoErrors` state array
- Track non-fatal errors (individual video failures) separately
- Use `searchIdRef` to avoid stale closure in `onComplete` callback
- Fatal errors (no videoId) set `error` state and stop streaming

**Frontend UI Updates (`app/routes/index.tsx`):**
- Display fatal errors in red error banner
- Display non-fatal video errors in yellow warning banner after completion
- Show count of failed videos: "X video(s) failed to classify"

### Acceptance criteria verified:
- [x] Single video classification failure emits `error` event and continues processing
- [x] `error` event contains `videoId` and `message`
- [x] Apify failure emits `error` event (no videoId) and closes stream
- [x] Frontend hook sets `error` state on fatal errors
- [x] Frontend displays error banner for fatal errors
- [x] Non-fatal errors (individual video failures) shown but don't block results
- [x] Long-running streams don't timeout (fetch handles this natively)

---

## Phase 5: Streaming Keyword Search - COMPLETED

Commit: 3a6803c

### What was built:

**Backend (`server/services/keyword-search-history/`):**
- Added `KeywordStreamingCallbacks` type
- Added `createKeywordSearchRecord()` - Create Search record at stream start
- Added `saveStreamingKeywordResult()` - Save single video + search result during streaming
- Added `finalizeKeywordSearch()` - Update timestamps at stream end
- Added `classifyVideosStreaming()` - Sequential classification with callbacks

**Backend Endpoint (`server/index.ts`):**
- Added `POST /api/search/stream` endpoint
- Same event schema as brand explorer (init, video, progress, complete, error)
- Returns cached results as JSON (no streaming overhead)

**Frontend (`app/routes/index.tsx`):**
- Unified `useStreamingSearch` hook for both keyword and brand explorer
- Removed `useSearchMutation` - both search modes now use streaming
- Simplified results logic - streaming results work for both modes
- Identical streaming UX for keyword search and brand explorer

### Acceptance criteria verified:
- [x] `POST /api/search/stream` endpoint streams keyword search results
- [x] Same event schema as brand explorer (init, video, progress, complete, error)
- [x] Keyword search videos saved incrementally to DB
- [x] `useStreamingSearch` hook works for both search modes
- [x] UI behavior identical between brand explorer and keyword search

---

## Phase 6: Cache-Aware Routing - COMPLETED

Commit: (no code changes needed)

### Analysis:

Cache-aware routing is already implemented by the existing architecture:

**History Selection (from sidebar):**
- Uses `useHistoryDetailQuery` → `GET /api/history/:id`
- Returns instant JSON response (no streaming overhead)
- React Query caches responses for subsequent requests

**New Searches:**
- Use `useStreamingSearch` → `POST /api/*/stream` endpoints
- Streaming endpoints check cache FIRST before initiating SSE
- If cached: return JSON immediately (no SSE overhead)
- If not cached: stream results via SSE

**Frontend Hook:**
- Detects response type via `content-type` header
- JSON (`application/json`): parses and sets all state at once
- SSE (`text/event-stream`): processes events incrementally
- Seamless UX regardless of cache status

**Response Format:**
- All cached responses include `cached: true` flag
- Available for logging/debugging in both JSON and complete event

### Acceptance criteria verified:
- [x] Frontend detects when results might be cached (history selection uses instant endpoint)
- [x] Cached requests use existing JSON endpoints (streaming endpoints return JSON for cached)
- [x] Uncached requests use streaming endpoints
- [x] Seamless UX regardless of cache status
- [x] `cached: true` flag available in response for logging/debugging

---

## All Phases Complete!

The streaming results feature is fully implemented:
- Phase 1: SSE infrastructure ✓
- Phase 2: Brand explorer streaming with DB persistence ✓
- Phase 3: Frontend streaming hook ✓
- Phase 4: Error handling ✓
- Phase 5: Keyword search streaming ✓
- Phase 6: Cache-aware routing ✓

---
---

# Insights Tab Feature

> Source: plans/insights-tab.md

---

## Phase 1: Toggle + Market Snapshot - COMPLETED

Commit: 7ec3f34

### What was built:

**InsightsPanel Component (`app/components/InsightsPanel.tsx`):**
- `ViewMode` type exported for toggle state (`'results' | 'insights'`)
- `computeMarketSnapshot()` function computes metrics from results array
- `MarketSnapshot` component displays 4 key metrics with loading states
- `InsightsPanel` main component with info bar and empty state handling

**Market Snapshot Metrics Computed:**
- Active brands detected (unique non-null brand values)
- Total creators (unique handles)
- Sponsored posts (count + percentage where `isSponsored` or `isAd`)
- Estimated reach (5-15% of unique creator followers sum)

**HomePage Integration (`app/routes/index.tsx`):**
- Added `viewMode` state with Results/Insights toggle
- Toggle button group in header bar matching visual pattern
- Conditional rendering: Results → DataGrid, Insights → InsightsPanel
- Both views receive same `data`, `keyword`, `isLoading`, `progress` props

### Acceptance criteria verified:
- [x] Toggle appears in header next to search input
- [x] Clicking "Insights" shows InsightsPanel, "Results" shows DataGrid
- [x] Market Snapshot displays all 4 metrics
- [x] Stats update in real-time as results stream (via useMemo dependency)
- [x] Works with both new searches and cached history items

---

## Phase 2: Brand & Influencer Blocks - COMPLETED

Commit: ccf7f4d

### What was built:

**Brand Breakdown Component:**
- `computeBrandBreakdown()` function aggregates brands with post counts
- Displays brands sorted by count descending with percentages
- Shows up to 8 brands in the list
- Empty state: "No brands detected yet"

**Influencer Clusters Component:**
- `computeInfluencerClusters()` function groups creators into two categories
- **Top by Reach**: Top 5 creators sorted by follower count
- **Likely Sponsored**: Top 5 creators with brand associations
- Each entry shows handle and formatted follower count
- Sponsored section shows associated brand name

**Types Added:**
- `BrandBreakdownItem`: brand, count, percentage
- `InfluencerItem`: handle, followers, brand, avatarUrl

**Layout Update:**
- Changed to 2-column responsive grid (`grid-cols-1 md:grid-cols-2`)
- Market Snapshot, Brand Breakdown, and Influencer Clusters in grid

### Acceptance criteria verified:
- [x] Brand Breakdown shows top brands with counts and percentages
- [x] Influencer Clusters shows two groups: top by reach, likely sponsored
- [x] Both blocks update progressively as results stream
- [x] Handles edge case: no brands detected (shows appropriate message)
- [x] Handles edge case: no sponsored content detected

---

## Phase 3: LLM Backend + Content Patterns - COMPLETED

Commit: a297bdf

### What was built:

**Backend Insights Service (`server/services/insights/`):**
- `insights.types.ts` - Types: InsightsInput, InsightsResult, ContentPatternsData, OpportunitySignalsData, SuggestedAction, InsightsError
- `insights.schema.ts` - Zod schema for structured LLM output with `.describe()` hints
- `insights.prompt.ts` - System prompt for content strategist role, dynamic user prompt builder
- `insights.ts` - `generateInsights()` function using Vercel AI SDK with gpt-4o-mini
- `index.ts` - Re-exports public APIs

**Backend Endpoint (`server/index.ts`):**
- `POST /api/insights` endpoint accepts `{ searchId }`
- Fetches captions from SearchResult or Video tables based on search type
- Calls LLM and returns structured InsightsResult
- Returns 400 if < 3 captions available

**Frontend Hook (`app/hooks/useInsights.ts`):**
- TanStack Query hook with infinite staleTime (cached forever per searchId)
- Only fetches when streaming complete and 3+ results exist
- Retry disabled for graceful error handling

**Frontend Component Updates:**
- `ContentPatterns` component displays themes and hooks from LLM
- Loading skeleton while LLM processes
- Error state with retry button
- Added `searchId` prop to InsightsPanel
- Conditional rendering: only shows after streaming completes

**Types Added (`app/types/index.ts`):**
- ContentPatternsData, OpportunitySignalsData, SuggestedAction, InsightsResult

### Acceptance criteria verified:
- [x] `/api/insights` endpoint returns structured LLM response
- [x] Content Patterns block shows themes extracted from captions
- [x] Content Patterns block shows top hooks/opening patterns
- [x] LLM insights only requested after streaming completes
- [x] Loading state shown while LLM processes
- [x] Insights cached per searchId (no re-fetch on tab toggle)

---

## Phase 4: Opportunity Signals + Suggested Actions - COMPLETED

Commit: 0f5a9f6

### What was built:

**OpportunitySignals Component:**
- Saturation level indicator with color-coded badges (low=green, medium=yellow, high=red)
- Market gaps list showing underserved areas
- Emerging opportunities list
- Loading skeleton while LLM processes

**SuggestedActions Component:**
- Prioritized recommendations with color-coded priority badges
- Each action shows rationale explaining why it's recommended
- Spans full width for better readability
- Loading skeleton while LLM processes

**Layout Updates:**
- Market Snapshot now spans full width (`md:col-span-2`)
- Market Snapshot uses 4-column grid on desktop (`md:grid-cols-4`)
- Suggested Actions spans full width (`md:col-span-2`)
- All 6 blocks now displayed in InsightsPanel:
  1. Market Snapshot (full width)
  2. Brand Breakdown
  3. Influencer Clusters
  4. Content Patterns (full width)
  5. Opportunity Signals
  6. Suggested Actions (full width)

### Acceptance criteria verified:
- [x] Opportunity Signals displays market gaps and saturation level
- [x] Suggested Actions shows prioritized recommendations with rationale
- [x] Full InsightsPanel now shows all 6 blocks
- [x] Layout: 2-column grid with Market Snapshot and Actions spanning full width

---

## Phase 5: Polish & Edge Cases - COMPLETED

Commit: 6324a83

### What was built:

**Loading States:**
- Added skeleton loaders to BrandBreakdown component
- Added skeleton loaders to InfluencerClusters component
- Pass `isLoading` prop to client-side computed blocks
- All blocks now show skeleton animations while loading

**Layout Stability (min-height):**
- MarketSnapshot: 100px
- BrandBreakdown: 180px
- InfluencerClusters: 240px
- ContentPatterns: 140px
- OpportunitySignals: 200px
- SuggestedActions: 160px

**Insufficient Data Handling:**
- Show helpful message when < 3 results available
- Message explains: "Need at least 3 results for AI-powered insights"
- Shows current count: "Currently analyzing X videos"
- LLM blocks hidden until sufficient data available

**Error Handling:**
- ContentPatterns has error state with retry button
- All LLM blocks share error state from useInsights hook
- Graceful degradation: client-side blocks always visible

### Acceptance criteria verified:
- [x] Each block shows skeleton loader while computing/fetching
- [x] LLM failure shows error state with retry button
- [x] Insufficient data (< 3 results) shows helpful message instead of LLM blocks
- [x] Switching between history items loads correct insights (searchId-based caching)
- [x] No layout shift as blocks load progressively (min-height)

---

## All Insights Tab Phases Complete!

The Insights Tab feature is fully implemented:
- Phase 1: Toggle + Market Snapshot ✓
- Phase 2: Brand & Influencer Blocks ✓
- Phase 3: LLM Backend + Content Patterns ✓
- Phase 4: Opportunity Signals + Suggested Actions ✓
- Phase 5: Polish & Edge Cases ✓
